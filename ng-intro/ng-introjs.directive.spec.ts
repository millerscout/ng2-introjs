import { IntrojsDirective } from './ng-introjs.directive';

describe('Ng2IntrojsDirective', () => {
  it('should create an instance', () => {
    const directive = new IntrojsDirective();
    expect(directive).toBeTruthy();
  });
});
